import {HTTP} from 'src/utils/httpUtils';

const BACKEND = '192.168.0.0';

export const login = (input) => {

  return HTTP.post();
}

export const logout = (input) => {

}

export const logout = (input) => {

}

